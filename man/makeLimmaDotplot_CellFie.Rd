% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/script.R
\name{makeLimmaDotplot_CellFie}
\alias{makeLimmaDotplot_CellFie}
\title{Make limma dotplot to visualize the significant metabolic tasks/subsystems/systems across cancers}
\usage{
makeLimmaDotplot_CellFie(
  rlst_list,
  adj.P.Val.cutoff,
  w = NULL,
  h = NULL,
  OUTDIRV,
  Depth = "Depth3",
  lowfigure = F,
  title = "Pan-cancer tasks",
  suffix = "Hu2025",
  taskHierarchy = cfs,
  nested = T,
  filter = FALSE,
  probs_cutoff = 0.35
)
}
\arguments{
\item{rlst_list}{named list. Output from `runLimmaCellFie` / `limmaTest_CellFie` containing `rslt_of_interest` data.frames per cancer.}

\item{adj.P.Val.cutoff}{numeric. Adjusted p-value cutoff for selecting significant tasks (e.g. 0.05).}

\item{w}{numeric or NULL. Figure width in inches; when NULL a sensible width is computed from the number/length of tasks.}

\item{h}{numeric or NULL. Figure height in inches; when NULL a sensible height is computed from the number of tasks.}

\item{OUTDIRV}{character. Directory where the dotplot PDF will be written.}

\item{Depth}{character. Which hierarchical column in `taskHierarchy` to use as the `Task` label (e.g. "Depth3").}

\item{lowfigure}{logical. If TRUE use compact theming suitable for small figures (default FALSE).}

\item{title}{character. Plot title (default: "Pan-cancer tasks").}

\item{suffix}{character. Filename suffix used when saving the plot (default: "Hu2025").}

\item{taskHierarchy}{data.frame. A mapping table that includes `Depth1`, `Depth2`, `Depth3`, and a `Task` column used to attach hierarchical labels.}

\item{nested}{logical. If TRUE facets are nested by `Depth1` (requires `ggh4x`).}

\item{filter}{logical. If TRUE filter tasks to the top frequent ones based on `probs_cutoff`.}

\item{probs_cutoff}{numeric. Quantile threshold used when `filter = TRUE` to select top tasks (default 0.35).}
}
\value{
A ggplot object (invisibly returned) and a PDF file written to `OUTDIRV`.
}
\description{
Create a dot-plot summarizing differential behavior of tasks (or
subsystems/systems) across cancer types using per-group limma results.
Each point shows the effect size (t-statistic) and adjusted p-value for
a task in a given cancer; point shape encodes direction (Up/Down) and
color maps the test statistic.
}
\details{
The function collates per-cancer `topTable` results into a long
table, annotates tasks with hierarchical labels from `taskHierarchy`,
and filters to tasks that are significant by `adj.P.Val.cutoff`. Points
are sized by adjusted p-value (smaller means more significant) and
colored by the test statistic `t`. When `filter = TRUE`, tasks are
reduced to those occurring above the `probs_cutoff` frequency quantile.
}
\examples{
\dontrun{
# p <- makeLimmaDotplot_CellFie(rlst_list, adj.P.Val.cutoff = 0.05, OUTDIRV = "./results", Depth = "Depth3", taskHierarchy = cfs)
# print(p)
}
}
